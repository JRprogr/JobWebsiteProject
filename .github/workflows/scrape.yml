name: scrape

# Scrapes every company whose interval has elapsed (hourly by default, big career sites every six hours, see
# source_config.every_hours in db/seed/companies.json). Runs here rather than on Vercel, which does not allow scrapers.
on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
    inputs:
      slugs:
        description: "Only these companies, space separated (empty = whatever is due)"
        required: false
        default: ""
      backfill:
        description: "Read many more listing texts per company (only with slugs)"
        type: boolean
        default: false

# One run at a time: a slow run must never overlap the next hourly one
concurrency:
  group: scrape
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  scrape:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci --omit=dev
      - name: Scrape
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NEXT_PUBLIC_SITE_URL: ${{ vars.SITE_URL }}
          NODE_OPTIONS: --disable-warning=MODULE_TYPELESS_PACKAGE_JSON
          # certificates some sites forget to send, see certs/intermediates.pem
          NODE_EXTRA_CA_CERTS: ${{ github.workspace }}/certs/intermediates.pem
          SLUGS: ${{ inputs.slugs }}
          BACKFILL: ${{ inputs.backfill && '--backfill' || '' }}
        run: |
          if [ -n "$SLUGS" ]; then
            node scripts/scrape.mts $SLUGS $BACKFILL
          else
            node scripts/scrape.mts --due
          fi
